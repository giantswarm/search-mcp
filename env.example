# Giant Swarm Search MCP Server Environment Configuration
# Copy this file to .env and fill in your actual values

# Note: Authentication is OPTIONAL. Without it, you can still search public documentation.
# With authentication, you can also access internal (intranet) resources.

# Session Cookie for intranet access:
# 1. Visit https://intranet.giantswarm.io/ and login with GitHub
# 2. Open Developer Tools (F12 or Cmd+Option+I) → Application → Cookies
# 3. Copy the '_oauth2_proxy' cookie value
INTRANET_SESSION_COOKIE=your_oauth2_proxy_cookie_value_here

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
# PYTHONLOGLEVEL=DEBUG
